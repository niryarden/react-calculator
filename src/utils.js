const stringToOperator = {
    '+': (x, y) => {return x + y},
    '-': (x, y) => {return x - y},
    '/': (x, y) => {return x / y},
    '*': (x, y) => {return x * y}

};

export default stringToOperator;